{
  "name": "web",
  "scripts": {
    "dev": "parcel src/index.html --dist-dir=../server/public",
    "watch": "parcel watch src/index.html --dist-dir=../server/public",
    "build": "rm -rf ../server/public && parcel build src/index.html --no-source-maps --dist-dir=../server/public"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "date-fns": "^2.30.0",
    "nprogress": "^0.2.0",
    "parcel": "^2.10.3",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6"
  }
}
