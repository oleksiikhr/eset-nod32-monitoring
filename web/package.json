{
  "name": "web",
  "scripts": {
    "dev": "parcel dev src/index.html --out-dir=../server/public",
    "watch": "parcel watch src/index.html --out-dir=../server/public",
    "build": "rm -rf ../server/public && parcel build src/index.html --out-dir=../server/public"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.2",
    "postcss": "^8.0.9",
    "tailwindcss": "^2.0.3",
    "date-fns": "^2.17.0",
    "nprogress": "^0.2.0",
    "parcel-bundler": "^1.12.4"
  }
}
